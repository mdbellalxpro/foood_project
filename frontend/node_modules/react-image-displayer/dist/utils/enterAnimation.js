const l = (e, i) => {
  if (i)
    return i;
  if (!e || e[0] === "none")
    return {};
  const t = { duration: e[1] };
  return e[0] === "scale" ? {
    initial: { opacity: 0.5, scale: 0.5 },
    animate: { opacity: 1, scale: 1 },
    transition: t
  } : e[0] === "fullscale" ? {
    initial: { opacity: 0, scale: 0 },
    animate: { opacity: 1, scale: 1 },
    transition: t
  } : e[0] === "fade" ? {
    initial: { opacity: 0 },
    animate: { opacity: 1 },
    transition: t
  } : e[0] === "blur" ? {
    initial: { opacity: 0, filter: "blur(10px)" },
    animate: { opacity: 1, filter: "blur(0px)" },
    transition: t
  } : e[0] === "left" ? {
    initial: { x: "-10%" },
    animate: { x: 0 },
    transition: t
  } : e[0] === "right" ? {
    initial: { x: "10%" },
    animate: { x: 0 },
    transition: t
  } : e[0] === "top" ? {
    initial: { y: "-10%" },
    animate: { y: 0 },
    transition: t
  } : e[0] === "bottom" ? {
    initial: { y: "10%" },
    animate: { y: 0 },
    transition: t
  } : e[0] === "fullleft" ? {
    initial: { x: "-100%" },
    animate: { x: 0 },
    transition: t
  } : e[0] === "fullright" ? {
    initial: { x: "100%" },
    animate: { x: 0 },
    transition: t
  } : e[0] === "fulltop" ? {
    initial: { y: "-100%" },
    animate: { y: 0 },
    transition: t
  } : e[0] === "fullbottom" ? {
    initial: { y: "100%" },
    animate: { y: 0 },
    transition: t
  } : {};
};
export {
  l as getFramerMotionProps
};
